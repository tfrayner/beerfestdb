// denied.js — access denied page (no grid functionality needed)
document.addEventListener('DOMContentLoaded', function () {
    // The denied TT template includes a message-panel div with the error message.
    // Nothing to initialise here.
});
