[connections.cbf]
dialect = "mysql"
host = "mysql"
port = 3306
database = "beerfestdb"
username = "beerfestdb"
password = "vent&T4p"

[auth]
# redirect_uri must be the browser-accessible external URL
redirect_uri = "https://titus.local:8444/oauth2callback"
cookie_secret = "randomtextgoeshere"

client_id = "dashboard"
client_secret = "my-secret"
server_metadata_url = "https://titus.local:8443/.well-known/openid-configuration"

[app]
# Inactivity timeout in minutes before the user is automatically logged out.
inactivity_timeout_minutes = 60
